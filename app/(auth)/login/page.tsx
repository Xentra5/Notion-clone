import LoginForm from "@/components/auth/login-form";

export const metadata = {
  title: "Log in | Notion",
  description: "Log in to your Notion workspace.",
};

export default function LoginPage() {
  return <LoginForm />;
}
